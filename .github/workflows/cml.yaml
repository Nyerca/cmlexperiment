name: train-my-model

on: [push]

jobs:
  run:
    runs-on: [ubuntu-latest]
  
    steps:
      - uses: actions/checkout@v2

      - uses: iterative/setup-cml@v1

      - uses: iterative/setup-dvc@v1

      - uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: cml
        env:
          GITHUB_TOKEN: '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'
          GDRIVE_CREDENTIALS_DATA: 'ya29.a0ARrdaM-R_7ZKXH0tEMeF0cnAbAfGVPknrINjyHamUXxdyUXR66NwA3LxKVvk9gsFwpAxhd1ygxJ6Gvyc4cPTYYST2nVvL4aJY8wZrMfkEJrnWxA_rt0bG0LvMcCHef8mx42yHwewRwc6AnNpVEQQ-YaP2-jn'
        run: |
          pip install -r requirements.txt
          
          # Pull dataset with DVC 
          dvc pull data
          
          # Reproduce pipeline if any changes detected in dependencies
          dvc repro 
          
          # Use DVC metrics diff to compare metrics to master
          git fetch --prune --unshallow
          dvc metrics diff --show-md master >> report.md
          
          # Add figure to report
          dvc plots diff --target loss.csv --show-vega master > vega.json
          vl2png vega.json -s 1.3 | cml-publish --md >> report.md
          cml-send-comment report.md 